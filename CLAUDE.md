# CLAUDE.md

This repository has been migrated to Codex project instructions.

The canonical agent guidance now lives in `AGENTS.md`, which Codex reads as the project-specific instruction file. Keep this file only as a legacy pointer for older Claude-based tooling.

When updating agent instructions, edit `AGENTS.md` first. If a Claude-only workflow still needs this file, keep it short and point back to `AGENTS.md` instead of duplicating instructions.
